<p style="color: green"><%= notice %></p>
<%= render @product %>

<%= turbo_frame_tag "sidepanel" do %>
    <div class="rounded-t-xl overflow-y-auto fixed top-3/5 bottom-0 left-0 right-0 bg-white p-4 s-xl [box-shadow:0_-4px_8px_rgba(0,0,0,0.08)]">
      <%= render "compact_product", product: @product %>
    </div>
<% end %>
